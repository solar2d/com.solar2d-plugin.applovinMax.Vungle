Sources for the plugin `plugin.applovinMax.Vungle`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.applovinMax.Vungle" = {
            publisherId = "com.solar2d",
        },
    },
}
```
